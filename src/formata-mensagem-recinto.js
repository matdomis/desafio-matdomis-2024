function formataMensagemRecinto(numero, espacoLivre, espacoTotal) {
  return `Recinto ${numero} (espaço livre: ${espacoLivre} total: ${espacoTotal})`
}

export { formataMensagemRecinto as formataMensagemRecinto }
